var assert = require("assert");
var timelinegrid = require("../src/js/timelinegrid");

describe('timelinegrid', function() {
    it('basic', function() {

        var timeline = timelinegrid.TimelineGrid({

        });
    });
});
